name: Build, Push, and Deploy 'latest' Docker Image to EKS

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v3
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Login to Amazon ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v2

    - name: Build, tag, and push image to Amazon ECR
      env:
        ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY_URI }}
        IMAGE_TAG: latest
      run: |
        docker build -t $ECR_REPOSITORY:$IMAGE_TAG .
        docker push $ECR_REPOSITORY:$IMAGE_TAG

    # --- Deployment Steps ---
    
    - name: Install kubectl
      uses: azure/setup-kubectl@v3
      with:
        version: 'v1.28.0' 

    - name: Update Kubeconfig for EKS
      run: |
        aws eks update-kubeconfig --name demo-cluster --region ${{ secrets.AWS_REGION }}
      
    - name: Deploy application to Kubernetes
      run: |
        kubectl apply -f deploy.yaml
        kubectl apply -f service.yaml
        kubectl rollout status deployment/your-deployment-name

    - name: Display Service Information
      run: |
        echo "--- Kubernetes Service Status ---"
        # This command's output will automatically appear in the 


